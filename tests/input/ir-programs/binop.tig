let
    var x := 1
    var y := 2
    var s1 := "hello"
    var s2 := "world"
    var z := y - x
    var w := y + x
    var a := x < y
    var b := s1 = s2
    var c := x <> y
    var d := s1 >= s2
    var e := "a" < "b"
in
    a + c + z + w
end