/* should have error on the second but not the first */
let 
    function a(x:int) =
        while x > 0 do
            /* x + 1 currently being evaluated to unit ... */
            x := x + 1
            if x = 10
            then break
in
    (a(11);
    break)
end
